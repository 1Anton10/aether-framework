/**
 * Aether meta package
 *
 *   import 'aether'           — overview
 *   import { renderToString } from 'aether/ssr'
 *   import { createRouter } from 'aether/router'
 */
console.info(
  "[aether] Wasm UI framework — use aether/ssr, aether/router, or the CLI (create / dev / migrate)."
);
export const version = "0.1.0";
